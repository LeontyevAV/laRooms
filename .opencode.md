# laRooms — Агрегатор отелей

## Стек
- **Framework:** Next.js 14+ (App Router, строго RSC)
- **UI:** Tailwind CSS + shadcn/ui
- **ORM:** Drizzle ORM
- **DB:** PostgreSQL
- **Cache:** In-memory (LRU Map с TTL)
- **Deploy:** VPS + PM2 + nginx
- **Auth:** Яндекс OAuth
- **API:** Яндекс Путешествия White Label

## Архитектура
```
Пользователь → nginx → Next.js (PM2)
                         ├── SSR/SSG страницы (RSC)
                         ├── API Routes → Яндекс API (прокси + кэш)
                         ├── Drizzle → PostgreSQL
                         └── In-memory кэш (поиск, suggest)
```

## TODO

### Инфраструктура
- ✅ Инициализация Next.js 14+ (App Router, TypeScript)
- ✅ Tailwind CSS + shadcn/ui
- ✅ Drizzle ORM + PostgreSQL (схема БД, миграции)
- ✅ PM2 конфигурация (ecosystem.config.js)
- ✅ nginx конфигурация (reverse proxy, SSL)

### API интеграция
- ✅ OAuth токен Яндекс API
- ✅ `/api/suggest` — прокси с debounce + кэш навсегда
- ✅ `/api/search` — прокси с кэшем (TTL 10 мин, ключ = хэш параметров)
- ✅ `/api/hotel/[id]` — прокси hotel + images + reviews
- ✅ `/api/hotel/[id]/offers` — прокси офферов
- ❌ `/api/booking/*` — прокси createOrder, payment, status, refund
- ✅ In-memory кэш (LRU Map)

### Основной функционал
- ✅ Главная: форма поиска (город/даты/гости)
- ✅ Автокомплит (suggest + debounce 300ms)
- ✅ Результаты поиска с фильтрами
- ✅ Классическая пагинация с URL (`/search?...&page=2`)
- ✅ Карточка отеля: фото, отзывы, карты, цены
- ❌ Бронирование через Яндекс API
- ❌ Статус бронирования
- ❌ Избранное + авторизация (Яндекс OAuth)

### SEO + Контент
- ❌ Мета-теги, sitemap.xml, robots.txt
- ❌ Скрипт-сидер: генерация URL городов и подборок в БД (для `generateStaticParams`)
- ❌ ISR-страницы: `/hotels/[city]`, `/hotels/[city]/with-pool`, и т.д.
- ❌ Markdown-движок для статей
- ❌ JSON-LD (Structured Data) для отелей

### Маркетинг
- ❌ Яндекс Метрика + Вебвизор
- ❌ Телеграм-бот
- ❌ Email-рассылки
- ❌ Яндекс Директ (ретаргетинг)
- ❌ Партнёрская ссылка `affiliate_clid`
